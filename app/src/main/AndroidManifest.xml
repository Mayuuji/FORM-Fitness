<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hfad.camera2">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Form">

        <!-- Launch / menu -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Workout screens -->
        <activity android:name=".PushupActivity"   android:exported="false"/>
        <activity android:name=".CurlsSetupActivity" android:exported="false"/>
        <activity android:name=".CurlsActivity"   android:exported="false"/>
        <activity android:name=".SquatActivity"   android:exported="false"/>

    </application>
</manifest>
